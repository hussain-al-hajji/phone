# برومبت: صفحة تفاعلية شاملة لدورة تدريبية مباشرة (ملف HTML واحد + Firebase)

> **طريقة الاستخدام:** انسخ هذا البرومبت كاملًا، واستبدل كل ما بين [أقواس مربعة] ببيانات دورتك، ثم أرفق معه **ملف المحتوى العلمي** (الوحدات والمحاور والشرائح والتمارين). ولا حاجة لإرفاق أي ملف HTML نموذجي.

---

## 0) البيانات المتغيرة (املأها قبل الإرسال)

| البند | القيمة |
|---|---|
| عنوان الدورة | [عنوان الدورة] |
| طبيعة التقديم | مباشر (لايف)، والمتدربون يشاركون من الجوال أو الحاسوب |
| عدد الوحدات | [5] وحدات، ولكل وحدة [3] محاور (التفاصيل في ملف المحتوى) |
| فصل إضافي | [مثال: الذكاء الاصطناعي وعلاقته بمجال الدورة] |
| رابط قاعدة البيانات | [https://xxxx-default-rtdb.firebaseio.com/] |
| الرمز السري للوحة الإدارة | [3719] |
| ألوان الهوية | [أرفق الشعار واطلب استخراج ألوانه، أو اكتب الأكواد: #0093A8، #3B4677، #00A653، #F58220، #FAB20B] |
| خط العناوين | [Cairo] |
| مدة بقاء التهنئة بعد انتهاء البرنامج | [3 أيام] |
| عدد المجموعات الافتراضي | [6] |
| التذييل (Footer) | [اسم المدرب] — [التعريف] — [الموقع الشخصي] |

---

## 1) الطلب الأساسي

أريد **صفحة ويب تفاعلية واحدة** (ملف HTML واحد قائم بذاته) لدورة «[عنوان الدورة]»، تحتوي على ما يلي:

1. محتوى علمي يُعرض كشرائح تتحرك يمينًا ويسارًا (بالسحب باللمس، وبالأسهم، وبلوحة المفاتيح).
2. تمارين تفاعلية حية يشارك فيها المتدربون من أجهزتهم، ويرون مشاركات بعضهم لحظيًا.
3. تصميم بصري مميز وغير تقليدي، فيه رسوم بيانية ومشاهد تعبيرية توضّح المحتوى.
4. لوحة إدارة كاملة تتيح للمدرب التحكم بالمشاركات والمحتوى نفسه من المتصفح، دون أن يفتح الكود بعد التسليم.

لك حرية إبداعية كاملة في الطابع البصري، بشرط الالتزام بألوان الهوية المحددة أعلاه وتحقيق كل المتطلبات الوظيفية أدناه.

---

## 2) هيكل المحتوى العلمي

### 2.1 القواعد العامة
- خذ الوحدات والمحاور والشرائح والتمارين من ملف المحتوى المرفق، بالترتيب نفسه.
- **لا تقسّم المحتوى حسب الأيام** في الواجهة.
- المحور هو وحدة التنقل الأساسية. في الصفحة الرئيسية تُعرض المحاور كشبكة واحدة، وقبل محاور كل وحدة **عنوان جانبي** فيه سطر صغير («الوحدة الأولى») وتحته **عنوان الوحدة الكامل**.
- على بطاقة كل محور تظهر شارة بعنوان الوحدة التي ينتمي إليها، لا بعبارة «الوحدة 1».
- لكل محور: عنوان إبداعي، وعنوان تقليدي (العنوان الأصلي في المقرر)، ووصف، ومدة، و«أبرز النقاط»، وأيقونة، ولون خاص به.
- أضف فصلًا عن «[الفصل الإضافي]» يشمل: اقتراحات الاستخدام، والأدوات والمنصات وتطبيقاتها، ومتى لا يصح الاعتماد الكلي عليه (حدود الاستخدام ودور المتخصص البشري).

### 2.2 أنواع الشرائح (ستة أنواع ثابتة)

| النوع | الحقول |
|---|---|
| افتتاحية | عنوان، نص رئيسي، «قاعدة تذكّرها» + المشهد التعبيري الخاص بالمحور |
| مبدأ علمي | عنوان، مقدمة، نقاط (سطر لكل نقطة)، «الرسالة / جملة الربط» |
| أمثلة | عنوان، عناصر بصيغة «العنوان :: التفصيل» |
| أخطاء وتصحيحات | عنوان، عناصر بصيغة «الخطأ :: التصحيح» |
| أدوات | عنوان، عناصر بصيغة «الأداة :: الاستخدام» |
| خلاصة | عنوان، نص، قاعدة + نسخة مصغّرة من مشهد المحور |

**حقول اختيارية لكل شريحة:**
- صورة.
- رابط مصدر للتوسع.
- رابط فيديو (انظر 6.12).

### 2.3 سلامة المحتوى (راجعه قبل البناء)
- **لا تتجاهل ملاحظات التصميم** الموجودة في ملف المحتوى (مثل «تصميم الشريحة: قمع / مسار أفقي / مصفوفة 2×2»). حوّلها إلى رسم بياني مناسب للشريحة، ولا تعرضها كنص.
- **أي مفهوم تبني عليه التمارين لا بد أن يُشرح في شرائح المحور نفسه قبل تمارينه.** مثال: إذا كانت تمارين المحور عن «الاحتكاك»، فيجب أن تسبقها شرائح عن تعريفه، ومصادره، وكيف يتحول إلى مغادرة، وأمثلة عليه، وكيف يُكتشف. إن لم يكن الشرح موجودًا في المقرر فاكتبه بنمط باقي الشرائح.
- إذا كانت إحدى الوحدات في المقرر غير مقسّمة إلى محاور داخل الشرائح، فقسّمها منطقيًا حسب الموضوع، وأخبرني بما فعلت.
- إن وجدت نقصًا يستدعي **إضافة محور جديد غير موجود في المقرر، فاستأذنّي أولًا**.
- راجع كل النصوص لغويًا، واستخدم صيغة المذكر في الحالات العامة دون افتراض جنس المتدرب.
- كل الأرقام في الموقع بأرقام إنجليزية (0-9)، بلا استثناء.

---

## 3) التمارين

### 3.1 القواعد العامة
- تمارين متنوعة لكل محور (لا تمرين واحد)، وكل تمرين يعمل على مهارة مختلفة.
- التمارين لا تظهر في الصفحة الرئيسية إطلاقًا؛ تظهر أسفل شرائح محورها فقط.
- حدّد لكل تمرين: فردي أم جماعي، ونموذج إجابته. إن لم يحدده المقرر فاختر الأنسب.
- **تجنّب تمارين الثنائيات** التي تحتاج شريكًا فعليًا؛ حوّلها إلى تمرين فردي يتقمص فيه المتدرب دورًا واحدًا، مع وصف الطرف الآخر داخل الموقف.

### 3.2 بنية صفحة التمرين (بهذا الترتيب)
1. صورة اختيارية أعلى الصفحة.
2. العنوان + أيقونة (إيموجي) + شارتا «فردي/جماعي» و«نموذج الإجابة».
3. 🎬 **الموقف / السيناريو** (مع رسم بياني إن لزم).
4. 🧭 **مستخلص المحور**: بطاقة تعريفية وسيطة فيها عنوان المحور وأبرز نقاطه (للتمارين المرتبطة بمحور فقط).
5. 🔬 **المبدأ العلمي باختصار**.
6. 🛠 **كيف تنجز التمرين؟** (خطوات مرقّمة؛ للنماذج التفاعلية خطوات افتراضية تلقائية).
7. 👥 **اختيار المجموعة** (للجماعي فقط)، وهو اختيار واحد يُحفظ للمتدرب ويسري على كل التمارين الجماعية.
8. 📝 **المطلوب منك** في مربع منفصل بصريًا بخلفية داكنة. في النماذج التفاعلية يحل محله «💡 تلميح عام».
9. **صندوق الإجابة**: بعد الحفظ يتحول إلى عرض للإجابة مع زر «تعديل» لصاحبها فقط.
10. **مشاركات الجميع مباشرة**، وتظهر بعد صندوق الإجابة مباشرة (انظر 3.4).
11. أسفل الصفحة صندوقان متجاوران: 🎯 «لماذا هذا النشاط؟» و🧩 «النموذج المساعد» (للتمارين النصية فقط، انظر 3.5).
12. شريط تنقّل (للتمارين المرتبطة بمحور فقط):
    - صف فيه «◀ التمرين السابق» و«📖 محتوى الفصل» و«التمرين التالي ▶»، وتحته زر «🏠 الرئيسية».
    - السابق والتالي يُحسبان من تمارين المحور الظاهرة فقط، ويُعطَّلان عند أول تمرين وآخره.

**أسفل قائمة تمارين المحور** (في صفحة المحور نفسها):
- صف فيه زرّا «الفصل القادم ▶» و«🏠 الرئيسية».
- «الفصل القادم» يحترم الترتيب الذي يحدده الأدمن، ويتخطى المحاور المخفية والمعطّلة، ويُعطَّل عند آخر محور.

### 3.3 نماذج الإجابة الخمسة

**أ. نصية حرة:** فردي أو جماعي.

**ب. اختيار من متعدد:** **فردي دائمًا**، ويعمل **بأسلوب التصويت المباشر**:
- الضغط على أي خيار **يحفظ الإجابة فورًا**، دون زر حفظ أو زر تعديل.
- يمكن التعديل في أي وقت بالضغط على خيار آخر في السؤال نفسه.
- يُحفظ كل سؤال على حدة: `answers/{i}` بتحديث جزئي. انتبه إلى أن مصفوفة الإجابات قد تكون **مبعثرة**، فاقرأها بدالة تحافظ على موضع كل إجابة، ولا تستخدم دالة تضغط المصفوفة وتُزيح الإجابات عن مواضعها.
- بعد أن يختار المتدرب في سؤال، تظهر بجانب كل خيار **نسبة من اختاره من المتدربين** مع شريط ملوّن، وتحت السؤال «👥 N مشاركة». النسب تتحدث حيًا.
- **لا تظهر النسب قبل أن يجيب المتدرب** عن السؤال، حتى لا تؤثر على اختياره. الزائر والأدمن يرونها دائمًا.
- لا توجد خانة «مشاركات الجميع» لهذا النموذج، لأن النتائج تظهر داخل الخيارات نفسها.
- التمرين يُعتبر منجزًا فقط عند الإجابة عن **كل** أسئلته.

**ج. صح أم خطأ:** فردي، مع حفظ ثم عرض للإجابة وتعديل.

**د. إكمال الفراغ:** **جماعي دائمًا**.
- بنك كلمات، وكل كلمة تُستخدم مرة واحدة.
- الطريقة: اضغط الكلمة، ثم اضغط الفراغ.
- **الضغط على فراغ ممتلئ دون تحديد كلمة يُفرغه ويعيد كلمته إلى البنك**، وإلا استحال تصحيح إجابة مكتملة.

**هـ. مقارنة نقيضين:** **جماعي دائمًا**. في كل زوج (أ) و(ب) يختار المتدرب العبارة الأدق.

**قواعد التمارين الجماعية التفاعلية (إكمال الفراغ ومقارنة النقيضين):**
- الأسئلة تظهر فورًا للاطلاع حتى قبل اختيار المجموعة، لكنها معطّلة مع ملاحظة توضح ذلك. بعد الاختيار يظهر زر «حفظ وإرسال إجابات المجموعة».
- بمجرد إرسال أي مجموعة، تظهر «إجابات المجموعات» مجمّعة أسفل الأسئلة في الصفحة نفسها. كل مجموعة تحت اسمها بإجابتها الكاملة، مع إبراز مجموعة المستخدم.
- عرض الخيار في قائمة إجابات المجموعات يستخدم صنفًا **مضغوطًا** (أصغر، بمحاذاة طبيعية)، لا أحجام صناديق الاختيار الكبيرة.
- عند فتح مجموعة لتمرين سبق أن أرسلته، **تُحمَّل إجابتها المحفوظة في مكانها** ولا تُفتح الصفحة فارغة.

**قواعد عامة للنماذج التفاعلية:**
- **كل تمرين تفاعلي يحتوي 10 فقرات على الأقل.** إن كان المقرر أقل فأكمله بفقرات من مفاهيم المحور نفسه، وحدّث نص الموقف إن كان يذكر عددًا («خمسة مواقف» تصبح «عشرة مواقف»).
- **كشف الإجابات:** زر «🔓 كشف الإجابات الصحيحة» في لوحة الإدارة يلوّن الإجابات لكل الزوار فورًا: الأخضر للصحيح، والأحمر للخطأ مع ذكر الإجابة الصحيحة بجانبه. ويجب أن يقرأ الحالة الفعلية من قاعدة البيانات قبل التبديل.
- الزائر ممنوع من أي تفاعل في هذه النماذج جميعًا.
- خزّن النموذج في حقل `format` على كائن التمرين نفسه (`text` / `mcq` / `truefalse` / `fillblank` / `comparePairs`) مع مصفوفة عناصره، حتى يعمل النظام تلقائيًا مع أي تمرين، أصليًا كان أو مُضافًا.

### 3.4 مشاركات الجميع مباشرة (للنصية ولصح أم خطأ)
- بطاقات فيها: اسم المشارك أو المجموعة، والمسمى، والوقت، والإجابة.
- زر إعجاب 👍 بعدّاد حي للمسجلين فقط.
- إبراز مشاركة المستخدم نفسه.
- زر حذف 🗑 لكل مشاركة يظهر للأدمن فقط.

### 3.5 النموذج المساعد
- **يظهر فقط في التمارين النصية**، و**يُحذف كليًا** من التمارين التفاعلية، ومن نموذج تحريرها في لوحة الإدارة أيضًا.
- **لا يحتوي على الإجابة الصحيحة أبدًا.** هو تلميح موجز: مثال من سياق مختلف يقرّب الفكرة، أو قالب يوجّه التفكير. مثال: «مستخدم يحجز تذكرة طيران ولا يعرف إن كانت الأمتعة مشمولة… طبّق الفكرة نفسها على الموقف».
- لا يظهر إلا بعد الضغط على «👁 أظهر النموذج المساعد».

### 3.6 أقسام خارج المحاور
- **«أنشطة»** في الصفحة الرئيسية، ولا تكتب «كسر الجليد» في عنوان القسم: بضعة أنشطة عامة لكسر الجليد، ويخفى القسم إن أُخفيت كل أنشطته.
- **«ختام البرنامج»** أسفل كل المحاور: استطلاع انطباعات بسيط، دون قسم تعليمات.

---

## 4) المختبر الختامي (مشروع تطبيقي شامل)
- مختبر واحد مدته **ساعة**، مقسّم إلى 6 مراحل (10 دقائق لكل مرحلة)، وكل مرحلة تغطي محورًا أو وحدة مختلفة، على **حالة افتراضية واحدة** بأرقام قمع تحويل.
- زر «🚀 ابدأ الوقت» **إلزامي** قبل ظهور أي صندوق إجابة.
- بعد البدء:
  - عدّاد تنازلي.
  - تظهر كل المراحل من البداية. المقفلة تعرض «🔒 باقي mm:ss على إتاحتها» وتُفتح تلقائيًا.
  - زر «إيقاف مؤقت / استمرار».
  - زر «إعادة ضبط» يعيد الوقت إلى الصفر بعد تأكيد، دون حذف الإجابات.
- الوقت محفوظ في قاعدة البيانات لكل مجموعة (`start` و`pausedAt` و`pausedTotal`)، ويبقى صحيحًا بعد التحديث أو من جهاز آخر. استخدم فرق توقيت الخادم.
- «متابعة كل المجموعات»: مربع لكل مجموعة فيه إجاباتها، مع إعجابات. يظهر حتى قبل اختيار مجموعة، ويُبنى من البيانات الفعلية المحفوظة.

---

## 5) تسجيل الدخول والهوية

### 5.1 شاشة الدخول
- الحقلان: اسم كامل، ومجال أو مسمى وظيفي.
- الأزرار: «ابدأ»، ثم زر بلون مختلف «مسجّل مسبقًا؟ الدخول برقم العضوية»، ثم «👀 تصفح كزائر».

### 5.2 رقم العضوية
- يُولَّد عند كل تسجيل **بعملية ذرّية** (Firebase transaction)، بصيغة 4 خانات على الأقل (0058).
- يُحسب بـ `Math.max(الحالي, حد أدنى) + 1`.
- العدّاد في مسار مستقل لا يتأثر بأي مسح.
- بعد التسجيل تظهر نافذة ترحيب بالاسم ورقم العضوية، وزر «حفظ رقم العضوية» يرسم بطاقة PNG (على canvas) ويحفظها مباشرة مع إشعار تأكيد.

### 5.3 الدخول برقم العضوية
- قراءة لمرة واحدة لسجل المسجلين، ثم مطابقة الرقم.
- تُستعاد الهوية بلا كلمة مرور.
- الرقم غير المطابق يعطي رسالة واضحة تطلب التسجيل من جديد.

### 5.4 حفظ الهوية
- بطبقات متعددة: رابط الـhash، وlocalStorage، وsessionStorage، والكوكيز.

### 5.5 الأزرار الثابتة
- زر ثابت مرئي دائمًا «تبديل المستخدم»، يمسح الهوية المحلية فقط.
- زر «حسابي» للمسجلين.

### 5.6 الزائر
- يرى كل شيء، وصناديق الإجابة معطّلة مع «🔒 للمسجلين فقط».

### 5.7 إعادة الضبط الشاملة
- تسجّل ختمًا زمنيًا، وأي متصفح هويته أقدم منه يُطلب منه التسجيل من جديد.

---

## 6) لوحة الإدارة

### 6.0 المبادئ
- الدخول بالرمز السري عبر **نافذة داخل الصفحة**، لا برابط مباشر. تبقى الجلسة في sessionStorage.
- **شاشة واحدة متصلة**، ليست صفحات منفصلة. نماذج الإنشاء والتعديل وحدها صفحات مستقلة، وتعود دائمًا إلى الشاشة الرئيسية للوحة.
- **كل التنبيهات والتأكيدات نوافذ HTML داخل الصفحة**، وليست `alert` أو `confirm` أو `prompt`، لأنها تُمنع صامتة داخل iframe.
- لا تعرض تفاصيل تقنية لا يحتاجها المدرب، مثل «وضع تجريبي محلي».

### 6.1 تنظيم الأدوات
- بطاقات متماثلة الحجم في شبكة `repeat(auto-fill,minmax(210px,1fr))`: نمط عنوان موحّد (أيقونة ملوّنة + نص)، والأزرار ثابتة أسفل البطاقة.
- الأدوات مصنفة تحت **لافتات أقسام** (شريط لوني + عنوان):
  - **إدارة المسجلين:**
    - المسجّلون: العدد، والقائمة، وزر «مسح الأسماء فقط» داخل البطاقة نفسها.
    - إدارة المجموعات.
    - معاينة كمتدرب.
    - تهنئة الإنجاز: العدد، والأسماء، ومعاينة، وتعديل المحتوى.
    - بث رسالة مباشرة.
  - **التصدير والنسخ:**
    - ملف المحتوى PDF.
    - تصدير CSV.
    - مشاركات فردية.
    - النسخة الاحتياطية.
- **المحتوى المنسدل** لأي بطاقة (قائمة، جدول، نموذج) يظهر مباشرة أسفل بطاقته، كعنصر `grid-column: 1 / -1` يأتي بعدها في الـHTML.
- **«إعادة ضبط شاملة»** إجراء إتلافي مستقل خارج كل الأقسام:
  - يمسح المشاركات والمختبر والمؤقتات والمسجلين والتعيينات.
  - **يستثني الاستطلاع الختامي**، فيقرأ شجرة المشاركات ويحذف كل مفتاح ما عدا معرّف الاستطلاع.
  - يفعّل ختم إعادة الضبط.
- **جرس إشعارات** ثابت في كل شاشات الإدارة:
  - نقطة بعدد غير المقروء.
  - تُبنى من بيانات التسجيل والمشاركات الموجودة أصلًا.
  - قائمة منسدلة توضح «من فعل ماذا ومتى».
  - تُعلَّم كمقروءة بعد لحظة.
  - «آخر مشاهدة» محفوظ في localStorage على جهاز المدرب فقط.

### 6.2 واجهة الصفحة الرئيسية
- **أكورديون مضمَّن** بلافتة خاصة، بلا صفحة منفصلة.
- الحقول:
  - عنوان الشريط العلوي ووصفه.
  - عنوان القسم البارز ووصفه وصورة الغلاف (اختيارية).
  - **عدد المسجلين المعروض**: عدّاد مستقل يزيد تلقائيًا، ويمكن تعديله أو تصفيره يدويًا.
  - التذييل: الاسم، والتعريف، والرابط، و5 روابط تواصل (LinkedIn، وX، وInstagram، وواتساب، والبريد). يتحوّل رقم واتساب تلقائيًا إلى `wa.me`، والبريد إلى `mailto:`، ولا تظهر أيقونة لحقل فارغ.
- زر «استرجاع الافتراضي».
- أي مكان يحتاج «عنوان الدورة» يقرؤه من عنصره في الصفحة وقت الحاجة.
- **التذييل يظهر في كل الصفحات**، بما فيها شاشة الدخول.

### 6.3 المحاور والتمارين
- قائمة المحاور **بالسحب والإفلات**، والترتيب محفوظ في مسار مستقل. أي محور جديد يُلحق بالنهاية تلقائيًا.
- كل محور **أكورديون مطوي افتراضيًا** بلونه، وخلفية مخففة، وشريط لوني، وأيقونته، وسهم يدور عند الفتح. أزرار المحور:
  - تعديل.
  - إظهار / إخفاء.
  - **مفعّل ⇄ معطّل**.
  - 🧬 نسخ.
  - «استرجاع الافتراضي» للمحتوى الأصلي، أو «حذف نهائي» للمُضاف، مع حذف متسلسل لتمارينه.
- كل تمرين **في صف واحد يجمع كل إجراءاته**:
  - تعديل.
  - إظهار / إخفاء.
  - 🧬 نسخ.
  - استرجاع أو حذف.
  - مسح المشاركات.
  - كشف الإجابات (للتفاعلي فقط).
  - وتحت الصف **خلاصة حية** بالمشاركات، مع زر حذف لكل مشاركة.
- المبدأ نفسه يُطبَّق على الأنشطة والاستطلاع الختامي.
- زرّان واضحان: «➕ إضافة محور جديد» و«➕ إضافة نشاط جديد».
- علامة **«مضاف»** للعناصر المنشأة من اللوحة، وعلامة «معدَّل» للمعدّلة. تظهران للأدمن فقط.

### 6.4 ثلاث حالات مستقلة تمامًا في مسارات منفصلة
| الحالة | المسار | الأثر |
|---|---|---|
| مخفي | `visibility/*` | يختفي كليًا (المحور من الشبكة، والتمرين من قائمته، والقسم إن فرغ) |
| معطّل (للمحاور فقط) | `enabled/*` | يبقى ظاهرًا بمظهر باهت وشارة «قريبًا»، ويعرض رسالة «غير متاح بعد» عند الضغط |
| معدَّل | `content/*` (تراكب) | لا يمسّ الحالتين الأخريين إطلاقًا |

- **كل العدّادات في الرئيسية ديناميكية** من العناصر الظاهرة فعليًا.

### 6.5 تعديل المحتوى من المتصفح (التراكب)
- **المصفوفات الأصلية في الكود لا تُعدَّل أبدًا.** كل تعديل يُحفظ في Firebase كتراكب (Override) منفصل ويُدمج عند القراءة.
- «استرجاع الافتراضي» يحذف التراكب فقط.
- أي حفظ يُنشر حيًا لكل من يتصفح، دون إعادة تحميل.
- **نموذج المحور:**
  - العنوان، والعنوان التقليدي، والوصف، والمدة، وأبرز النقاط، والأيقونة (قائمة مع معاينة)، والصورة.
  - الشرائح: إضافة أي عدد أو حذفه أو ترتيبه (↑↓)، مع اختيار نوع كل شريحة. عند تغيير النوع تُحفظ حقول الأنواع الأخرى حتى لا تضيع.
  - الشرائح الافتراضية الست نقطة بداية فقط.
- **بعد إنشاء محور جديد:** انتقل مباشرة إلى صفحة تعديله. أسفل الشرائح قائمة «تمارين هذا المحور» مع «➕ أضف تمرينًا لهذا المحور»، وينطبق هذا على كل محور، أصليًا كان أو مضافًا.
- **نموذج التمرين:**
  - العنوان، والأيقونة، ونموذج الإجابة، والنوع، والسيناريو، والمبدأ، والخطوات، والمطلوب، والتلميح، و«لماذا»، والنموذج المساعد (للنصي فقط)، والصورة.
  - **قالب عناصر** يتغير حسب النموذج، مع «➕ أضف عنصرًا» و«🗑 حذف» بلا حد أدنى أو أقصى.
  - **«نوع العمل» يُقفل تلقائيًا**: فردي للاختيار من متعدد ولصح أم خطأ، وجماعي لإكمال الفراغ ولمقارنة النقيضين.
- **نموذج النشاط:** عنوان، وفردي أو جماعي، وسيناريو اختياري، والمطلوب، ونموذج مساعد اختياري.
- اكتب داخل الواجهة صراحة: «الرسوم البيانية داخل الشرائح تبقى قابلة للتعديل عبر الكود فقط».

### 6.6 محرر النص المنسّق
- للحقول الطويلة الحرة فقط: الوصف، والنص الرئيسي، والقاعدة، والمقدمة، والسيناريو، والمطلوب، والنموذج المساعد.
- الأدوات:
  - حجم الخط.
  - لون النص (منتقي ألوان).
  - عريض، ومائل، وتسطير.
  - محاذاة يمين ووسط ويسار.
  - قائمة نقطية أو رقمية.
  - مسح التنسيق.
- المحرر عنصر `contenteditable`، ويُحفظ `innerHTML` مع تنظيف من السكربتات والمعالجات.
- **الحقول المبنية على «سطر لكل عنصر» تبقى textarea عادية بلا تنسيق.**
- ⚠️ **لا تستخدم `preventDefault` على `mousedown`** لأي أداة هي عنصر تحكم أصلي (قائمة منسدلة، أو منتقي لون…). الطريقة الصحيحة: تتبّع آخر نطاق تحديد داخل المحرر باستمرار، عبر أحداث `keyup` و`mouseup` و`input` و`focus` و`touchend`، ثم استعده يدويًا لحظة استخدام أي أداة قبل تنفيذ الأمر.

### 6.7 رفع الصور
- صورة اختيارية بحد أقصى **5MB** لكل من: المحور، وكل شريحة، وكل تمرين أو نشاط، وغلاف الصفحة الرئيسية.
- تحقّق من النوع والحجم قبل الرفع، مع رسالة واضحة عند الرفض.
- تُحفظ **Base64 داخل قاعدة البيانات**. نبّه في الواجهة إلى أن هذا يكبّر حجم القاعدة، وأن ربط Firebase Storage خطوة لاحقة ممكنة.
- **بيانات الصورة أثناء التحرير في متغيّر JavaScript**، لا في خاصية HTML.

### 6.8 أدوات إضافية
- **تصدير CSV** لكل الإجابات (المحاور، والأنشطة، والاستطلاع، والمختبر) بترميز UTF-8 مع BOM، حتى تظهر العربية صحيحة في Excel.
- **بث رسالة مباشرة**: تظهر شريطًا أعلى الصفحة لكل المتصفحين، ويغلقها كل متصفح بشكل مستقل (localStorage حسب معرّف الرسالة).
- **نسخة احتياطية / استيراد** للمحتوى فقط (التراكب، والإضافات، والإظهار، والتفعيل، والترتيب، والواجهة، والإعدادات) في ملف JSON، مع تأكيد قبل الاستبدال.
- **🧬 نسخ** أي محور، ويشمل نسخ شرائحه وتمارينه، أو أي تمرين.
- **معاينة كمتدرب**: الرئيسية كما يراها المتدرب، مع شارة عائمة «↩ العودة للوحة الإدارة» بلا إعادة إدخال الرمز.

### 6.9 إدارة المجموعات (بطاقة واحدة موحّدة)
- البطاقة فيها: حقل العدد (2–30، والافتراضي [6]) مع زر حفظ، وزر «توزيع». لا رقم أو عدّاد على البطاقة نفسها.
- أسماء المجموعات **تُولَّد ديناميكيًا** («مجموعة 1»…). تغيير العدد يسري فورًا على كل التمارين الجماعية والمختبر، وتقليله لا يحذف بيانات المجموعات القديمة.
- **نافذة التوزيع:** أكورديون لكل مجموعة، فيه:
  - اسم مخصّص اختياري، يُحفظ تحت رقم المجموعة؛ وإفراغه يعيد عرض الرقم وحده.
  - بحث فوري بالاسم (تصفية من جهة المتصفح).
  - قائمة المسجلين غير المعيّنين كمربعات اختيار. الاختيار يضيف المتدرب فورًا بتحديث جزئي، ويخفيه من كل القوائم.
  - مربع الأعضاء الفعليين مع زر إزالة لكل عضو.
  - زر «إلغاء كل التعيينات».
- **التعيين إرشادي لا إلزامي:**
  - كل أزرار المجموعات نشطة دائمًا.
  - تنبيه فوق الأزرار يذكر اسم المجموعة المخصَّصة.
  - بعد الاختيار تظهر أسماء أعضاء تلك المجموعة.
  - بلا أي تعيين، لا يظهر أي تنبيه.
  - في المختبر يظهر التنبيه فقط، دون قائمة الأعضاء.

### 6.10 صفحة «حسابي» (للمسجلين فقط)
- تعديل الاسم والمسمى، ويُحدَّثان في الموقع وفي قاعدة البيانات.
- رقم العضوية ظاهر دائمًا.
- **نسبة الإنجاز الإجمالية** بارزة مع شريط تقدّم.
- **أوسمة**: وسام لكل محور، بتصميم ميدالية موحّد بلون المحور وأيقونته. يُفتح عند إنجاز **80%** من تمارين المحور، وقبل ذلك يظهر رماديًا باهتًا مع قفل.
- الحساب **يستثني** المحاور المخفية والمعطّلة والتمارين المخفية، بسطًا ومقامًا، ويُعاد حيًا.
- قسم **«تهنئة إنجاز»** (بهذا الاسم تحديدًا):
  - مقفل مع نسبة الإنجاز حتى بلوغ 80% إجمالًا.
  - يعرض **الاسم فقط** بلا مسمى، وعنوان الدورة، والتاريخ.
  - **هذه الصفحة هي المكان الوحيد** الذي تظهر فيه التهنئة.
- زر «تحميل / حفظ كـPDF» بصيغة **A4 أفقي** عبر المحرّك الموحّد (6.11)، **لا عبر `window.print()`**.
- زر «إرسال نسخة لبريدي» (`mailto:`)، يوضح صراحة أن الملف لا يُرفق تلقائيًا ويطلب إرفاقه يدويًا.
- تحت التهنئة إشعار بأنها ستختفي بعد [3 أيام] من انتهاء البرنامج.
- **محتوى التهنئة قابل للتعديل من اللوحة:** العنوان، والإيموجي، وفقرات بلا عدد إلزامي، وتذييلان، ونص الإشعار. يدعم الرموز `{{name}}` و`{{courseTitle}}` و`{{date}}`، مع زر استرجاع الافتراضي.
- في اللوحة: عدد من حصل على التهنئة وأسماؤهم، وزر معاينة.

### 6.11 محرّك PDF موحّد
- **المكتبات:** html2canvas وjsPDF وJSZip، تُحمَّل من CDN **عند الحاجة فقط**.
- **آلية بناء الصفحات:**
  - كل صفحة `div` خارج الشاشة بمقاس ثابت بالبكسل (A5 عمودي 559×794، وA4 أفقي 1123×794).
  - تُحوَّل إلى صورة بمقياس أكبر من 1 (نحو 1.6، وJPEG بجودة 0.85).
  - تُضاف إلى jsPDF بوحدة المليمتر (A5: 148×210، وA4 أفقي: 297×210).
- ⚠️ `await document.fonts.ready` **قبل كل** استدعاء لـhtml2canvas، وإلا تتشوه الحروف العربية.
- ⚠️ **الأيقونات داخل صفحات PDF تُبنى كـSVG ذاتي الاكتفاء** من قاموس JavaScript (المسارات الخام)، **لا عبر `<use href>`**، وإلا تظهر فارغة.
- ⚠️ **لا تستخدم `<ol>`/`<li>` للترقيم في صفحات PDF.** ابنِ الترقيم يدويًا (رقم داخل شارة + نص بـflex)، حتى يبدأ من اليمين.
- ⚠️ استثنِ واجهة التطبيق من نسخة html2canvas (`ignoreElements`) لتسريع الالتقاط.
- **تثبيت المحتوى في صفحة واحدة:** صغّر نسبة خط الحاوية تدريجيًا حتى يتسع المحتوى أو يصل إلى حد أدنى معقول.
- **ملف المحتوى (A5)** في صفحة «حسابي»، وبمعاينة من اللوحة. بنيته بالترتيب:
  1. غلاف أمامي.
  2. فهرس، بتوزيع متساوٍ على صفحاته دون قطع أي بطاقة.
  3. لكل محور: غلاف بلونه وأيقونته وأشكال زخرفية، ثم صفحة لكل شريحة بهيدر ملوّن كامل.
  4. غلاف خلفي ببيانات المدرب.
- **تفاصيل ملف المحتوى:**
  - الغلافان بخلفية فاتحة متعددة الألوان من ألوان الهوية، وبطاقة بيضاء، وأيقونات محاور متناثرة.
  - نصوص المحتوى بمحاذاة مضبوطة الجانبين (Justify).
  - **لا روابط أو فيديو** في الملف، وهو يُبنى من أحدث نسخة للمحتوى.
  - في اللوحة بطاقة لبيانات الملف وتفعيل الميزة أو تعطيلها.
- **مشاركات فردية لكل متدرب (PDF + CSV):**
  - بطاقة في اللوحة فيها بحث، وزرّا PDF وCSV أمام كل اسم، و«تصدير الكل» في ملف ZIP.
  - **يُستبعد أي محور أو تمرين لم يشارك فيه المتدرب.** متدرب بلا أي مشاركة يحصل على صفحة أو صف توضيحي واحد.
  - الإجابات التفاعلية ملوّنة حسب الصحة دائمًا.
  - الجماعي يأخذ إجابة المجموعة **المعيّنة** للمتدرب، مع وسم «(ضمن مجموعة X)».

### 6.12 مرفقات الشرائح
- رابط مصدر مع نص اختياري، والافتراضي «مصدر للتوسع»، ويفتح في تبويب جديد.
- رابط فيديو من يوتيوب أو Drive يتحول إلى **مشغّل مضمَّن متجاوب** بنسبة 16:9 (عبر padding-top).
- دالة التحويل تتعرف على: `watch?v=`، و`youtu.be/`، و`shorts/`، و`embed/`، و`file/d/…/view`، و`open?id=`.
- الرابط غير المدعوم يظهر كزر «مشاهدة الفيديو».

---

## 7) المتطلبات التقنية الحرجة
- **ملف HTML واحد، JavaScript خام (Vanilla)**، بلا أي إطار عمل. الملفات الخارجية المسموحة فقط: Firebase (compat) وGoogle Fonts، ومكتبات PDF عند الحاجة.
- **Firebase Realtime Database** للمشاركات، والتسجيل، والمؤقتات، والمسجلين، والمحتوى. تفعيل الاتصال يعتمد على `databaseURL` فقط.
- **طبقة محاكاة محلية** (localStorage) بالواجهة البرمجية نفسها (`watch` و`get` و`set` و`update` و`remove` و`push` و`transaction` و`now`)، تعمل تلقائيًا بلا إعدادات، أو بإضافة `?demo=1` إلى الرابط للمعاينة دون لمس البيانات الحقيقية.
- ⚠️ **تفادي TDZ:** المحاكاة تُطلق المراقبات **بشكل متزامن** فور تسجيلها، لذا صرّح بكل متغير تستخدمه المراقبات قبل تسجيلها، وسجّل المراقبات في دالة إقلاع في نهاية الملف. **اختبر في وضع المحاكاة تحديدًا.**
- ⚠️ القراءة لمرة واحدة تستخدم **علمًا بوليانيًا** («هل نُودي؟»)، لا استدعاء دالة الإلغاء من داخل تعريفها.
- **غلّف** `history.pushState` و`replaceState` وlocalStorage وsessionStorage والكوكيز بـ`try/catch` مع بديل في الذاكرة.
- **Firebase يحوّل المصفوفات إلى كائنات** ويحذف المصفوفات الفارغة، فطبّع القراءة دائمًا (دالة `arr()`)، مع دالة منفصلة تحافظ على المواضع للإجابات المبعثرة.
- **زر رجوع المتصفح** (`pushState` و`popstate`)، وزر رجوع مرئي **يُشتق من بيانات العنصر الحالي**: من التمرين إلى صفحة محوره، ومن المحور إلى الرئيسية.
- **لا زر طباعة عام** ولا `window.print()`.
- **كل الأرقام إنجليزية (0-9).** لا تستخدم `toLocaleString('ar')`.
- **تصميم متجاوب بالكامل ودعم RTL كامل**، بلا تمرير أفقي على شاشة 390px.
- عند العرض داخل **iframe** يظهر شريط فيه زر «فتح في تبويب مستقل» (`location.href`).
- **لا تنشره كـArtifact**، لأن الـArtifact يمنع الاتصال بـFirebase. سلّمه ملفًا عاديًا يُرفع إلى Netlify Drop، أو Google Sites عبر «Embed URL».
- **إعادة الرسم الحي تحافظ على المدخلات:** احفظ قيم الحقول (`data-keep`) والتركيز قبل كل إعادة رسم ثم أعدها. لا تعِد رسم نماذج التحرير أثناء الكتابة. احفظ حالة الواجهة (الشريحة الحالية، والأكورديونات المفتوحة، والمسودات) في الذاكرة.
- ارتفاع عارض الشرائح = **ارتفاع الشريحة الحالية**، وليس ارتفاع أطولها.

---

## 8) الرسوم البيانية والمشاهد التعبيرية

### 8.1 مكتبة رسوم SVG مخصّصة
- الأنواع: مسار أفقي متعرّج، ومسار عمودي، وقمع (مع النسب بين المراحل)، وبطاقات بإيموجي، ومقارنة عمودين، وعنصر مركزي بعمودين، ودورة، وخطوات مرقّمة، وأعمدة (تدعم القيم السالبة)، ومصفوفة 2×2، وميزان، وخط زمني، وفجوة «المتوقع مقابل الفعلي»، ومعادلة، وشجرة قرار، وجدول.
- ⚠️ **كل نص عربي داخل `<foreignObject>`** فيه `div dir="rtl"` بعرض كامل صريح و`text-align:right`. **لا `<text>` بإحداثيات ثابتة** للنص العربي.
- ⚠️ **لا توزيع دائري للتسميات** عند أكثر من 4–5 عناصر. استخدم **عمودين** حول عنصر مركزي، بمسافة أمان ثابتة (نصف القطر + 20).
- قدّر ارتفاع الصناديق من طول النص (عدد الأسطر المتوقع)، ولا تثبّته.
- **اختبر برمجيًا** (Node.js) أن كل رسم ينتج مخرجًا بلا `undefined` أو `NaN` مع **بيانات المحتوى الفعلية** وحالات حدّية (عنصر واحد طويل).

### 8.2 المشاهد التعبيرية
- أسلوب مسطح موحد: هاتف + شخصية + عناصر رمزية، **بلا نص داخلها**.
- مشهد خاص لكل محور (في الافتتاحية، ونسخة مصغرة في الخلاصة).
- مشهدان عامّان لشرائح الأخطاء والأدوات.
- صورة غلاف للدورة أعلى الصفحة الرئيسية.

### 8.3 تنسيق النص تلقائيًا
- أبرز المصطلح قبل «:» أو «—» في نقاط المبدأ.
- أبرز الجملة الأولى في الافتتاحية والخلاصة.

### 8.4 الخطوط
- عريض ومميز للعناوين: **[Cairo]**.
- نظيف للواجهة والأزرار: IBM Plex Sans Arabic.
- مريح للفقرات: Noto Sans Arabic.

### 8.5 الألوان
- **كل ألوان المشروع من الهوية المحددة.**
- ألوان المحاور **عائلة لونية لكل وحدة**، بدرجات مختلفة لكل محور.
- الأحمر للأخطاء فقط.
- طبّق الألوان في كل مكان: الواجهة، والرسوم، والمشاهد، وملفات PDF، وبطاقة العضوية.

---

## 9) طريقة البناء المقترحة (لسهولة الصيانة)
- اكتب المحتوى في **ملفات نصية بصيغة بسيطة**: محور، شريحة، تمرين، `q:` / `tf:` / `fb:` / `cp:`، و`chart:`.
- اكتب سكربت Node يحوّل هذه الملفات إلى JSON، ويتحقق من:
  - عدد العناصر (10 على الأقل للتفاعلي).
  - توافق النموذج مع النوع.
  - عدم تكرار كلمات بنك الفراغات.
  - وجود `___` في كل جملة فراغ.
- اجعل سكربت البناء يجمع JSON والـCSS وملفات JS في `index.html` واحد.
- ضع المصادر وسكربت البناء في المستودع مع README يشرح النشر وقواعد Firebase.

---

## 10) التسليم والتحقق

### 10.1 قبل التسليم
- اختبر في متصفح حقيقي (Playwright) **في وضع المحاكاة**:
  - التسجيل، ونافذة الترحيب.
  - الشرائح.
  - النماذج الخمسة، ومنها **التصويت مع النسب** و**إفراغ فراغ ممتلئ**.
  - المختبر.
  - «حسابي».
  - لوحة الإدارة، وكشف الإجابات.
  - ملف PDF للمحتوى، والتهنئة، وملف ZIP للمشاركات الفردية، وCSV.
  - عرض الجوال 390px.
- لا أخطاء في وحدة التحكم.

### 10.2 مع التسليم
- **ملف `index.html`** جاهز.
- **قواعد Firebase المطلوبة:**
```json
{ "rules": { ".read": true, ".write": true } }
```
- تنبيه: أي عنصر عدّله المدرب من اللوحة يبقى بنسخته المحفوظة، ويحتاج «استرجاع الافتراضي» ليأخذ تحديثات الكود.

---

## 11) الفوتر (في كل الصفحات)
[اسم المدرب]
[التعريف]
[الموقع الشخصي]
